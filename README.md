# App Receitas

Projeto inicial gerado manualmente (Vite + React, JavaScript).

Para rodar:

```powershell
cd "C:/Users/User/OneDrive/Área de Trabalho/estudos/projetos-2025/projrtos-js/projetos-novembro/app-receitas"
npm install
npm run dev
```

Scripts:

-   `npm run dev` — servidor de desenvolvimento (Vite)
-   `npm run build` — build de produção
-   `npm run preview` — preview do build gerado
